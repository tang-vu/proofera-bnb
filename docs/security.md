# ProofEra security review and threat model

Updated: 2026-08-14. Scope: marketplace browser/server, live data adapters, BNB Agent Studio workers, Altana session lifecycle, PancakeSwap execution and evidence operations.

Status labels below are `implemented`, `building`, or `release gate`. Financial-risk disclosure is not treated as a substitute for a technical control.

## Assets and trust boundaries

Protected assets are user funds, admin passkeys, scoped session signers, API/RPC credentials, reviewed policies, transaction state, evidence integrity and the ProofEra brand/agent identity.

- User boundary: a browser passkey remains device-bound. ProofEra never asks for or receives its private material.
- Worker boundary: the autonomous secp256k1 signer exists only in a dedicated encrypted secret/KMS. General web runtimes, browser storage, logs and the marketplace database receive public material only.
- Network boundary: registry metadata, RPC/API responses, agent endpoints and transaction callbacks are untrusted until parsed and independently checked.
- Onchain boundary: BSC receipts and contract reads establish state; a submitted UI operation or relay response alone does not.

## High-priority threats and controls

| Threat                                     | Consequence                                           | Control                                                                                                                                                                                                                                                                                                                | Status                                                                                                                                                                               |
| ------------------------------------------ | ----------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Browser/server key exposure                | Theft of admin or autonomous authority                | Passkey browser boundary; worker/KMS scoped signer; redaction and bundle tests; no key fields in wire schemas                                                                                                                                                                                                          | building                                                                                                                                                                             |
| Ambiguous Altana grant retried             | Duplicate grant/fees or inconsistent authority        | Stable pre-generated descriptor; exact ready/submitting transition; atomic append-only one-shot claim; `outcome_unknown`; no retry until exact authority probe                                                                                                                                                         | 16 exact-SDK and 17 candidate-store tests pass; canonical store deployment and ceremony remain                                                                                       |
| Allowed selector with malicious arguments  | Wrong NFT/token/recipient/ticks or unconstrained loss | Canonical direct-call decode/re-encode plus exact plan binding for recipient, position, tokens, ticks, values, minimums and deadlines                                                                                                                                                                                  | pure semantic gate implemented; live plan/code checks remain                                                                                                                         |
| Proxy or delegatecall implementation drift | Reviewed runtime hash fronts different mutable logic  | Restrict M1 to independently reviewed non-proxy contracts, or pin proxy implementation and admin state at the same canonical block and surface that identity                                                                                                                                                           | release gate; runtime-code hash alone is insufficient                                                                                                                                |
| Generic router or nested dispatcher        | Session bypasses intended strategy bounds             | Deny multicall, Universal Router `execute`, Permit2, selfPermit, sweep/refund and generic approve/transfer by default                                                                                                                                                                                                  | implemented in the direct-call gate                                                                                                                                                  |
| Duplicate spend rules                      | Aggregate cap exceeds preview                         | Unique token/period entries and aggregate comparisons in canonical policy                                                                                                                                                                                                                                              | implemented in the pure policy gate                                                                                                                                                  |
| Stale/manipulated quote                    | Adverse price or expired range decision               | Server-owned current time, block/source observation checks, 60-second quote TTL, exact minimums and revalidation at confirmation                                                                                                                                                                                       | pure context gate implemented; live simulation remains                                                                                                                               |
| Malicious/spoofed agent metadata           | Unsafe execution target, phishing or XSS              | Registry identity is not capability; React text escaping; bounded fields/body; safe locator schemes; reviewed execution manifest independent of metadata                                                                                                                                                               | building                                                                                                                                                                             |
| SSRF through endpoint/image checks         | Internal network/credential access                    | Resolve and reject loopback/private/link-local IPs, recheck redirects/DNS, allow ports/schemes, cap body/time/type                                                                                                                                                                                                     | release gate before probing endpoints                                                                                                                                                |
| Replay/double submit/two tabs              | Duplicate operations                                  | Server-nonce-authenticated context/quote IDs, exact intent binding, nominal atomic durable reservation, canonical policy hash, stable operation/idempotency keys, ready-only begin and bound `callsId` reconciliation                                                                                                  | PostgreSQL boundary/live concurrency pass; deployed store and browser E2E remain                                                                                                     |
| Forged/copyable pool-init authority        | Custody signing without exact owner confirmation      | Exact phase-minus-one audit triplet; clean generation-2 release policy; predecessor fence + attempt ID; private snapshot-B instantiation; same-process owner-v5 TTY challenge; OS-CSPRNG nonce; exact response; metadata-only preclaim custody; closure-private ceremony/signature/broadcast capabilities              | Owner-v4 confirmation for `336af296` expired at the exact claim-only incident and cannot cross the fence; no generation-2 owner-v5 confirmation or transaction exists                |
| Release bootstrap tamper or race           | Unaudited bytes execute under an accepted release ID  | Absolute PowerShell 5.1 phase minus one; QuickEdit off through exact console-mode bits without input read/flush/injection; exact 13-value Process env (11 host + 2 WS guards); pinned Node/direct-Git executables; constrained Git; clean commit/tree/source/dependency and ignored-topology checks; exact child spawn | PS1 and PowerShell/.NET pre-scrub startup, OS/filesystem/process, console semantics, and loaded runtime/DLL remain trusted; no active same-user/all-race defense                     |
| Frozen console after durable claim         | Authority expires with ambiguous post-claim progress  | Exact incident raw-SHA admission only; fresh snapshot A; immediate durable reread; append-only `O_EXCL` `superseded_before_worker` fence; mandatory process restart; distinct later snapshot B; all non-exact states block                                                                                             | One-off QuickEdit incident ended `POST_CLAIM_RECHECK_OUTCOME_UNKNOWN`; console output/harness and later no-effect RPC check were not retained, so they are not reproducible evidence |
| Crash/restart after pool submission starts | Duplicate broadcast or false terminal receipt         | Submission journal v3 in retained submission-v2 namespace; generation-2 recovery quartet; durable `submission_started`; post-ack terminal dual-RPC reread; no retry/replacement; receipt-block post-state and provider-attested `F1 -> C1/ancestry -> C2 -> F2 -> EIP-1898(C)`                                         | private one-send path implemented; RPC semantics are not cryptographic/Byzantine finality proof; no pool transaction occurred                                                        |
| Unsafe test token presented as value       | Misleading liquidity or uncontrolled capital loss     | Reject names/configuration as trust; source/control review every token; label deployed PTA non-economic, unpriced and illiquid; unsigned initializer plan keeps all price/oracle/liquidity/authority gates open                                                                                                        | 14 existing pools rejected; isolated PTA/runtime/receipt proof passes; eligible pool absent                                                                                          |
| Revoke displayed too early                 | User believes authority is gone                       | Relay-confirmed revoke remains pending; only fresh, exactly bound absent-authority evidence produces `confirmed`                                                                                                                                                                                                       | pure lifecycle implemented; testnet proof remains                                                                                                                                    |
| Fixture leaks into judged path             | Fabricated live/performance claim                     | Strict default data mode; production rejects fixture mode; publishability and realized-field tests                                                                                                                                                                                                                     | partially implemented                                                                                                                                                                |
| API/RPC outage                             | False zero or stale claim                             | Explicit unavailable state; bounded cache; last-good data labelled stale; never silent fixture fallback                                                                                                                                                                                                                | building                                                                                                                                                                             |
| Metadata URL/open redirect                 | Phishing or browser exploit                           | Construct source links from known origins; HTTP(S)/IPFS typed locators; `rel=noreferrer`; no arbitrary redirect                                                                                                                                                                                                        | partially implemented                                                                                                                                                                |
| Cross-site requests to mutations           | Unwanted grant/execute bootstrap                      | Same-origin/CSRF tokens, origin validation, short TTL, nonce, authenticated user+wallet binding                                                                                                                                                                                                                        | release gate before mutations                                                                                                                                                        |
| Supply-chain/license issue                 | Build compromise or unlawful distribution             | Exact versions/lock, age gate, audit, source-boundary secret scan, production-license artifact and SHA-pinned CI actions                                                                                                                                                                                               | partially implemented; SBOM and Altana license resolution remain                                                                                                                     |

The repository scanner asks Git for tracked and non-ignored untracked paths; it never recursively opens ignored local state. Forbidden `.env*` files other than the root example, `.studio`, wallet directories, keystores and key/PEM filenames are reported before file access. If Git is unavailable it fails without walking the workspace. CI checkout credentials are not persisted, and license evidence is generated after verification in runner-temporary storage with commit, tool-version and SHA-256 provenance. This is defense in depth, not a substitute for a mature secret detector/SBOM before release.

Tooling must never print an entire process environment or package-manager configuration: registry tokens can be inherited from user-level config and emitted even when the repository contains no secret. Use only narrowly scoped non-secret queries, keep CI logs redacted, and rotate any credential that appears in tool output. The source scanner reports rule/path only and never matched material.

### Local BSC testnet deployer custody

A dedicated development EOA exists for the approved chain-97 PTA deployment at `0x997cD959798F7c925076eaeFF5855C5C2c1e5A49`. Its private key was generated with viem's secp256k1 CSPRNG path, encrypted as Web3 Secret Storage v3 with scrypt (`N=131072`, `r=8`, `p=1`) and AES-128-CTR, and stored outside the repository. The independent 48-byte random keystore password is wrapped with Windows DPAPI `CurrentUser`; the local directory has inheritance disabled and one effective allow rule for the current Windows SID. A second process independently unwrapped the password, verified the keystore MAC, recovered the same address and recovered an offline EIP-191 signature. Only public records are retained: the [custody record](../evidence/development/bsc-testnet-deployer-wallet-2026-08-12.json), [funding record](../evidence/development/bsc-testnet-pta-funding-2026-08-12.json), and [finalized deployment receipt](../evidence/development/bsc-testnet-pta-deployment-2026-08-12.json).

This is a temporary testnet custody boundary, not an Altana admin/session wallet, user funding wallet, production KMS, hardware wallet or mainnet authority. An EVM key is not chain-bound. DPAPI does not protect against malicious code running as the same Windows user or a sufficiently privileged administrator, and the blob is not a portable backup. The exact one-shot deployment consumed nonce `0`; its account nonce is now `1`, the contract is finalized, and the journal prevents another signature for that operation. The residual testnet balance is not blanket authority for any further transaction. Never reuse the key on mainnet.

The server-only readiness probe at `@proofera/integrations/server/bsc-testnet-deployer-custody` accepts only an absolute custody-directory path; it does not read configuration from environment variables. The chain, address, filenames, encrypted-artifact hashes and exact Windows PowerShell path/hash are code-pinned. It rejects relative/UNC/reparse/hard-linked custody artifacts, enforces bounded stable-handle reads and a current-user-only ACL, repeats path/hash/ACL validation after unlock, parses an exact duplicate-free Web3 v3 envelope, caps scrypt before use, verifies the MAC in constant time and derives the address without creating a wallet client. DPAPI input/output is bounded and carried through subprocess pipes, never argv; timeout/abort waits for process-tree termination or latches `cleanup_unknown`. Owned high-sensitivity buffers are overwritten on handled exit paths as a best-effort measure, although JavaScript/native/OS memory cannot prove destruction of every transient copy. The probe is therefore local-development hardening, not protection against same-user malware and not a signing capability.

The pool-initialization preactivation path deliberately uses a narrower metadata probe than that full readiness operation. Before the signing journal durably records `worker_started`, it checks only the fixed path, `lstat`/file kind, `realpath`, and exact current-user ACL. It does not open the keystore or DPAPI blob, invoke DPAPI, decrypt, validate a keystore MAC, derive an address, or reconstruct key material. The authority object therefore identifies only the current Windows user's fixed-path ACL possession; it does not establish custody-key ownership or signer identity. Only the post-claim signed-transaction recovery/attestation may verify the expected sender.

The read-only deployment observer was not a signing capability. Its pure envelope validator labelled caller-supplied but internally consistent data only `validated`; the server coordinator independently read two fixed official testnet origins and labelled a funded result only `observed`. The actual one-shot composition separately required a fresh in-process authority, append-only journal claim, isolated worker, strict custody revalidation and post-sign sender/transaction recovery. An ambiguous parent result never authorized replacement signing: the committed raw transaction was reconciled by its deterministic hash. The exact nonce-`0` deployment is now consumed and the account nonce is `1`; neither its envelope hash, observation digest, unsigned payload nor repository evidence authorizes another signature.

### Operational incident: exposed npm registry credential

On 2026-08-11, a local package-manager configuration diagnostic emitted an npm authentication token into tool output. The credential value is deliberately not reproduced or retained in repository documentation. It must be treated as compromised even though source-boundary scans do not find a repository copy. Required account-owner actions are to revoke/rotate it, review npm access/publication history for unexpected activity, invalidate any duplicated use, and confirm completion before public release. Replacement credentials must stay in the owning credential store and must not be supplied to ProofEra code or logs. Repository work now prohibits environment and package-manager configuration dumps; this process control does not prove that account-side rotation occurred.

## Session-key protocol

Required bootstrap record fields are user/session ID, Altana wallet, session public key/address, canonical policy hash, nonce, issued/expiry times and state. The worker creates the key before the grant, stores it encrypted, and deletes orphaned secrets after a short TTL. Only its reviewed public descriptor crosses the API boundary.

The pure worker lifecycle implements that boundary with an opaque secret handle, domain-separated binding hashes, injected clock/ID/nonce sources, a non-retryable unknown state, exact authority verification, replay/freshness checks, and TTL-gated idempotent cleanup. The marketplace browser projection omits the handle and provider response. The grant adapter additionally requires the exact persisted transition, an explicit configured RP ID matching the production WebAuthn credential, a curve-valid P-256 public key, and an atomic one-shot claim before SDK invocation. Its sanitized response contains no signer, credential, secret handle, SDK function, raw error, or `callsId`. The append-only PostgreSQL 17 claim ledger binds bootstrap/idempotency/bootstrap-hash/submission-hash independently and never reclaims identifiers. Its canonical migration, independent catalog/ACL/namespace/role-setting verifier, same-pool application gateway, exact deployment UUID check, SQL result limits, timeouts, no retry, acknowledged commit/rollback, and ambiguous-outcome sanitization pass 68 focused and 18 real PostgreSQL tests. A stalled verifier client is destroyed, and a post-timeout operation cannot continue to INSERT or COMMIT. The package exports only the server composition; raw SQL/verifier/transaction internals stay private. This is implementation evidence, not production durability: no database, KMS/secrets provider, authenticated/origin-checked endpoint, or log-redaction proof is deployed.

The tested permission-preview model and server-renderable component show the wallet, BSC testnet chain 97, target/function rows, raw-unit token caps with decimals, expiry, fee token, emergency behavior and revoke consequence, labelled by enforcement owner. They are not yet wired into the `/lp-activate` route or a wallet ceremony, so the current browser journey must not claim that a grant was previewed.

After a successful SDK return, and after every ambiguous return, the worker independently reads wallet keys/Keystore registration. A resolved SDK grant advances only to `authority_pending`. The worker enables execution only if wallet, curve/public key, byte-exact permissions, expiry and policy hash match. Unknown or mismatched authority disables action. Altana SDK 0.7.0's grant call does not expose `callsId`; a pending exception cannot be blindly reconciled through that ID.

Revocation requires the admin passkey. Requested/pending/unknown revocation retains an active warning. Only fresh invalid-authority reads make it revoked. Emergency documentation must include direct explorer/contract fallback steps after the exact SDK path is proven.

## PancakeSwap execution policy

Milestone 1 uses direct reviewed V3 Position Manager functions only. A manifest entry binds chain ID, contract address, deployed code hash or verified implementation identity, exact signature and selector, allowed token pair and semantic validator version. The reviewed-deployment schema separately derives `reviewId` from the canonical complete source/review metadata, fee/tick spacing, token decimals and runtime-identity manifest; any mutation breaks the content address. This prevents an approved snapshot from being silently altered, but it does not establish source verification, non-proxy status, implementation/admin immutability, token behavior or release approval.

The pure calldata gate currently permits only canonical, direct `mint`, `increaseLiquidity`, `decreaseLiquidity` and `collect` calls to the official BSC testnet Position Manager. It rejects non-canonical encodings, trailing data, nested dispatch, unknown selectors and any native-BNB value. It binds each decoded argument to a separately validated server plan without converting financial values through JavaScript numbers. A ready result is not authorization to submit: the live runtime must still rebuild the plan from current evidence, verify code identity and authority, simulate, and rerun the gate immediately before signing.

Runtime-code identity has its own fail-closed reader. It accepts only a previously bound block number/hash/timestamp, verifies that block by hash, and issues EIP-1898 `eth_getCode` reads with `requireCanonical: true`. Provider rejection is unavailable and never falls back to `latest` or a block number. The reader returns only byte lengths and Keccak runtime hashes; a match does not prove verified source, proxy implementation, upgrade safety, or behavior.

Position authority has an independent fail-closed reader. It binds the commit-pinned official chain-specific Position Manager, NFT ID, intended controller, block number/hash/timestamp, and server clock. The freshness policy and credential-free provider identity belong to trusted reader configuration and cannot be widened by request data. `ownerOf`, `getApproved`, and `isApprovedForAll` are canonical 32-byte ABI reads at the exact EIP-1898 block hash. Owner, token-level approval, and operator approval are reported separately; unauthorized is a valid observed state, while a revert, malformed word, chain mismatch, stale/future block, invalid reader configuration, or provider failure remains unavailable. The read grants no authority and must be repeated immediately before submission.

Manager constructor relations and token decimals have a separate fail-closed exact-hash reader. It verifies `factory()` and `deployer()` against the commit-pinned deployment, records nonzero `WETH9()`, and reads canonical `uint8` decimals from the exact token order supplied by the block-pinned pool snapshot. Freshness and provider provenance are server configuration. Sequential exact-block success establishes only those returned values; it does not establish token economics, transfer behavior, runtime-code identity, source verification, proxy safety, or permission to execute.

The pool-initialization owner ceremony is a local, non-executing security boundary. It displays the complete decoded release, review, envelope, transaction, caps and risk acknowledgement in the same process that would consume the result; adds a fresh 32-byte nonce from Node's operating-system CSPRNG; and accepts only exact UTF-8 confirmation bytes. The runtime separates an exact `300`-second envelope, at most `240` seconds for owner entry, an exact `45`-second execution-authority lifetime, and a `30`-second post-claim recheck freshness bound. The strict owner deadline is `min(challengeIssuedAt + 240 seconds, envelopeExpiresAt - 45 seconds)`, leaving the full execution reserve. Pre-recovery v4 domains bound those clocks; generation-2 v5 domains additionally bind the exact predecessor fence and attempt ID, so an expired v4 response cannot cross the fence. Only after exact byte match does the internal clock capture actual `confirmedAt`; the closure-branded command binds that timestamp and `executionExpiresAt = confirmedAt + 45 seconds`. A refreshed post-claim observation/authentication timestamp must be `>= confirmedAt`, preserve that same expiry, and satisfy the 30-second freshness check. These clocks do not alter the five-minute frame deadline or numeric transport limits; generation 2 uses v3 policy challenge/frame domains. The ceremony accepts no approval from argv, environment variables, files, logs, chat, or persisted predecessor bytes. The local native bridge holds the current-user fixed-custody-path/ACL object, one-attempt ceremony command, and fresh/reserved/consumed execution-capability lifecycle in closure-private state. That object asserts ACL possession only, not key ownership or signer identity. Capability consumption is terminal after the active signing journal durably starts the worker; expected-signer identity is verified only by the post-claim signed attestation. This makes copied structures non-authorizing; it does not prove protection from malicious same-user code or replace release review.

The first real policy-TTY attempt had two `GO` audits for exact release commit `1a89ddae5e1f575b39dda6134d3ccedaddda0adf`, tree `b28a12f78861183ece5ff1c3fce36c0abf094239`, and runtime-manifest SHA-256 `0xd5212c7563d5d3e3b10d461b5dedcc1552766262c1c87a7057b4dc2876ed0ce0`. Its canonical policy digest was `0x9357b560a91b1c41ef8ef84ea39001c206f50fc9839a55102f7916152160a189`; `8,670` policy bytes became `11,560` unpadded-base64url characters and an `11,713`-character v1 single-line frame. Windows Console truncated that frame, and the reader failed closed before admitting the policy or reaching owner confirmation. Only read-only recovery/journal inspection may have occurred. No fresh RPC, custody-artifact access, DPAPI operation, custody unlock, signature, new transaction hash, send, receipt, pool, or liquidity resulted. After the source changed to v2, the v1 triplet and policy became historical, non-authorizing evidence.

Current policy admission uses v3 challenge/frame domains over the strict nonce-bound ASCII `BEGIN`, exact ordered `CHUNK`, and `END` state machine introduced in v2, under one absolute five-minute deadline. Every line binds line index, exact chunk count, policy byte length, and lowercase raw policy-byte SHA-256; chunk lines also bind chunk index. Only LF or CRLF is accepted. The hard content-line cap is `4,096` bytes; maximum valid worst-case construction is exactly `2,618` bytes and is conservatively tested at `<= 2,700`. The transport is at most `102,400` bytes and the policy `1..65,536` bytes. Every non-final payload has exactly `2,304` unpadded-base64url characters and the derived chunk count is at most `38`. The historical `11,560`-character policy maps to six chunks/eight bounded lines and remains non-authorizing. A one-off local Windows Console observation motivated these conservative constants, but its harness and result are not retained and it is not reproducible repository evidence. Repository unit tests establish only the transport arithmetic and parser behavior. Exact reconstruction, canonical base64url, declared length, and hash must all match. Retired v1/v2 domains, truncation, duplicate/missing/reordered lines, control/blank input, and trailing or buffered bytes fail before policy admission, fresh RPC, custody access, or owner authority. This parser invariant must not be described as proof against OS-queued pre-input, malicious same-user preload, or parent-provenance substitution.

A later exact v2 local-terminal exercise used release commit `36f6e5e7fa8b4b5ccf255a6210afa2d25c25afa5`, tree `54ed5c2a8f754e79080528a2ce25669a6532a66b`, runtime-manifest SHA-256 `0x25e5aedb6d73e6fff416803ce9d42737d9124e525e0b81bf906321f4d06258d4`, and canonical policy digest `0xd1a33479a607d744a51ff8d6d3df8772f41ec2f1363911d2655f926c754c3b38`. The strict v2 reader admitted that policy; both recovery probes observed their signing/submission journals absent, fresh read-only preflight against the two fixed official RPCs succeeded, and the exact owner challenge was displayed. No exact owner confirmation was accepted. Later attempts stopped fail-closed with `POLICY_FRAME_INVALID`, `CEREMONY_IO_FAILED`, and `OWNER_CONFIRMATION_INVALID`. No custody artifact was accessed, no DPAPI/unlock occurred, and no signature, new transaction hash, send/broadcast, receipt, pool, or liquidity resulted. This was a one-off local terminal observation. Its raw output and harness were not retained, so it is not reproducible repository evidence or a gate; the negative custody/sign/send statements combine the observed fail-closed result with code ordering rather than a retained artifact. The timing source then changed, so that triplet and policy are historical and non-authorizing.

The next one-off local operational exercise used audited release `336af2967286795dc7703fff85034c71b8e84b5c`, tree `86cc383388982dac1a2bea430f54d54e56bb6cf9`, runtime-manifest SHA-256 `0xa1cda6fcf00f8a7d2b9a679cfb9b3fc28aa60674dae89c7dbfc032bdbcff5bdd`, and canonical policy digest `0x8ddae3b13ee64ff5f983ce30d06c84d671e0a0ca029f75b5482de6b34b18ba54`. The admitted `8,826` bytes had raw SHA-256 `0x87de481e35a0d8fe6c503f4a7c832d665699ac9f20aa79eb5c40471d79e71a45` and six chunks. Exact owner-v4 confirmation succeeded; the legacy journal durably retained only its `1,123`-byte slot-1 claim, raw SHA-256 `0xf10e90eb836a94446ace100bbc9a6fc5de6cc35b1d82e4d10fb4736ef8559e32`. QuickEdit/selection then froze console progress during post-claim RPC. Cancelling selection after authority expiry produced the observed `POST_CLAIM_RECHECK_OUTCOME_UNKNOWN` code without a transaction hash. Exact journal inspection and code ordering establish no worker authorization/start, signed/terminal slot, custody-artifact access, DPAPI/unlock, signature, or send; submission state was exact-empty. The raw console output and operational harness were not retained, so the owner/terminal observations are not reproducible repository evidence or a release gate. A later live dual-RPC no-effect check was likewise non-retained and cannot supply recovery freshness or authority. No receipt, pool, or liquidity exists.

The only admissible supersession is recovery generation 2 for that exact raw claim. Invocation A obtains fresh snapshot A only after legacy authority expiry, immediately rereads the legacy, active, and submission namespaces, appends an `O_EXCL`/fsync/self-hashed `superseded_before_worker` fence, rereads it, and must stop with `PREDECESSOR_FENCE_RECORDED_RESTART_REQUIRED` before policy, TTY, custody, signing, or submission. Invocation B must be a separate process, retain the exact fence and exact-empty active/submission state, admit a fresh committed/audited policy through TTY v3, obtain a distinct snapshot B strictly after the fence, instantiate the admitted policy on B, and only then request exact owner-v5 confirmation for the fence-bound attempt. Any later legacy slot, worker/signature/submission evidence, partial/mixed state, unexpected file, ACL/path ambiguity, race, or unknown append result fails closed permanently rather than becoming a retry.

The `00f21c4` external-review-request generator, its test, and retained artifact remain pinned to the historical 45-second envelope cap. They are historical-only inputs, not a producer or evidence source for the current `300`/`240`/`45`/`30` timing contract.

The authoritative process starts with `C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe -NoLogo -NoProfile -NonInteractive -ExecutionPolicy Bypass -File C:\Users\tangm\Documents\GitHub\proofera-bnb\scripts\run-bsc-testnet-pta-wbnb-pool-phase-minus-one.ps1 --release-commit <AUDITED40> --release-tree <AUDITED40> --runtime-manifest-sha256 <AUDITED0x64>`, with cwd exactly `C:\Users\tangm\Documents\GitHub\proofera-bnb`. Phase minus one hardcodes/requires that root, its own absolute path, and the absolute phase-zero path. Direct Node invocation is unsupported: ordinary ambient invocation is exact-env rejected, but phase zero cannot prove parent provenance against malicious preload/same-user code. The configured `pnpm initialize:pta-wbnb:testnet` command is a fail-only wrapper that returns nonzero. Phase minus one first clears its Process environment and sets exactly 13 pinned non-secret entries (11 real-host values plus two WebSocket native-addon disable guards). It then clears only `ENABLE_QUICK_EDIT_MODE`, sets `ENABLE_EXTENDED_FLAGS`, verifies all other console-input-mode bits unchanged, performs no input read, flush, synthesis, or injection, and invokes the pinned Node executable. Phase zero rejects any other environment, validates the audit triplet, pinned Node/direct `D:\Git\mingw64\bin\git.exe` executable files, constrained Git, clean published commit/tree, enumerated source blobs including the phase-minus-one PS1, pinned dependency trees and ignored runtime topology, and working directory, then starts phase one with the same environment.

Apart from the exact two console-mode bits and its own Process environment, phase minus one is operationally read-only; phase zero is read-only. Phase-one journal recovery is the first application-stateful or external operational action and precedes policy, TTY, RPC, custody-artifact access, signing, or send. This boundary trusts both the phase-minus-one bytes before their later manifest check and phase-zero bytes before/during their own checks, all Windows PowerShell 5.1/.NET startup behavior and ambient environment until scrub, OS/filesystem/process/console semantics, pinned executable files, and their loaded OS/runtime/DLL installation. The later self-check and Git-blob checks are evidence rather than a root of trust. It does not hash complete host installations, prove parent provenance, or actively exclude malicious preload/same-user code or every concurrent race.

Phase one reads the immutable legacy signing, active generation-2 signing, and submission journals before it can consider release policy, owner authority, custody or fresh RPC work. Active signing records use schema/namespace v2. The submission directory retains its v2 name but persists generation-2 v3 records and the exact `{ generation, predecessorState, predecessorFenceSha256, attemptId }` binding. Durable `submission_started` or `unknown_outcome` can only reconcile; a retained terminal result is reconstructed without another send; a signed commit without durable start cannot recreate owner authority. After a new durable start acknowledgement, a second fixed dual-RPC read must still match the exact nonce/pool/candidate/simulation/balance/gas evidence and be later than the acknowledgement before the closure-private one-send opportunity. Confirmation requires identical transaction/receipt/log evidence and EIP-1898 post-state at the receipt block. Each fixed provider must also attest the receipt-plus-128 checkpoint through `F1 -> C1/ancestry -> C2 -> F2 -> EIP-1898(C)`. The finalized heads may differ, but checkpoint, ancestry, post-state, and canonical-attestation results must match across providers. This sandwich relies on provider RPC semantics; it is not cryptographic proof that checkpoint `C` is an ancestor of `F1`/`F2` and is not proof of Byzantine independence. The command is executable only after a newly committed/pushed generation-2 release receives exact audits, a matching review policy, and the owner's separate bounded v5 TTY confirmation; the generic raw sender and public worker factory remain hard-blocked.

Immediately before each submission ProofEra rechecks:

- BSC testnet/mainnet, contract address/code identity and selector;
- wallet recipient, LP token ID ownership and expected token pair;
- tick spacing, ordered bounds and maximum range change;
- liquidity/amount bounds, token decimals and aggregate spend cap;
- quote observation time, maximum age and TTL, slippage/minimum amounts and deadline;
- execution count/rate, policy expiry and current session authority;
- simulation outcome and decoded calldata equality with the previewed policy hash.

Altana enforces target/selector, spend and expiry, not these arguments. If a judge-facing claim requires argument enforcement against a compromised worker, ProofEra will add and audit a narrow guard contract or narrow the claim.

## Data and web security

- External responses are parsed with strict/bounded schemas and bounded time/body limits. Raw payloads stay server/evidence-side.
- User-controlled query values are finite enums; no query/cookie may select fixture publication or execution targets.
- Names/descriptions render as text. External links use known HTTPS origins. Future endpoint fetches require SSRF controls and never forward credentials across redirects.
- Current headers include CSP framing/base/object restrictions, HSTS, nosniff, referrer, opener/resource and permissions policies. CSP allowances must be tightened alongside the final Altana browser endpoints.
- Sensitive mutations require authenticated same-origin requests, CSRF/origin checks, rate limits, replay nonce and audit events.
- Logs exclude authorization headers, API keys, cookies, WebAuthn payloads that reveal unnecessary material, private/session keys and raw signed calldata where it creates risk.

## Operational response

If a session or agent is suspected compromised: disable new executions, mark the marketplace agent suspended, request admin revoke, observe onchain invalidity, remove endpoint discovery, preserve non-secret audit evidence, rotate worker/API credentials, investigate affected policy hashes and publish an incident note. Never claim funds are safe until authority and balances are read.

## Required adversarial verification

- Public-descriptor grant on exact SDK 0.7.0; pending-to-late-confirm without retry; orphan cleanup; byte-exact authority comparison.
- Rejected/pending/failed/confirmed execute and revoke; reload, crash, double-click and two-tab idempotency; revoked/expired key cannot execute.
- Wrong contract/selector/code hash, dispatcher overloads, malicious recipient/token ID/tokens/ticks/minimums/deadline, duplicate caps and decimals mismatch.
- 8004scan empty/429/502/timeout/schema drift/huge response; stale last-good; malicious metadata/URL/SVG/redirect and production fixture-leak.
- Final-host virtual WebAuthn E2E plus a manual real-device create/recover/grant/revoke ceremony.

## Open release gates

- Durable HTTPS origin and exact RP ID are not configured. Production rejects IP, loopback, special-use, and public-suffix-only names; a real-browser ceremony on the final host remains required.
- No real Altana wallet/session/grant/execute/revoke receipt exists yet; the passing grant tests inject a signer-shaped object and never invoke WebAuthn or a network.
- The Altana LP handoff now has a concrete server-only PostgreSQL `consumeOrRead` implementation. Its versioned append-only migration, externally pinned PostgreSQL 17 catalog/ACL/rule digest, nominal administrator-verification capability, least-privilege application pool, bounded transaction outcomes and 10 real-PostgreSQL 17.9 cases pass. Activation rejects structural callback lookalikes, joins policy/attestation block number+hash+timestamp, uses monotonic clocks, and discards nested ready state if final receipt or target freshness fails. No production database is provisioned, so persisted bootstrap, live authority and production replay protection remain unavailable.
- Pancake intent and direct-calldata validation are implemented. Write-target attestation v2 isolates four direct Position Manager selectors and explicitly denies the manager's reachable self-`DELEGATECALL` multicall dispatchers. The public initializer bytes prove publication only; the unsent request pins `00f21c4`, and the owner-designated internal decision pins only nonexecuting `bc7000e`. Neither is external, Sigstore-authenticated, or authenticated-third-party review. Canonical WBNB and deployed fixed-supply PTA identities are evidenced, but neither is pool evidence. One-off release `336af296` received policy admission and owner-v4 confirmation, then stopped at the exact durable claim-only QuickEdit incident; raw console output/harness were not retained, so the observed owner/terminal path is not reproducible repository evidence. The expired confirmation cannot authorize recovery. The phase-minus-one QuickEdit control, two-invocation exact-incident fence, separate active signing-v2 journal, retained submission-v2 directory with v3 records, owner-v5 ceremony, private one-consume signing/broadcast bridge, post-ack reread, receipt-block post-state, and provider-attested finality sandwich are implemented with the limitations above. The recovery release still needs a committed/pushed identity, new exact audits and policy, distinct snapshot B, and separate owner-v5 confirmation. This document records no generation-2 owner approval, custody unlock for this operation, signature, send, pool receipt, pool, liquidity, or LP position.
- Durable Agent Studio hosting and four live BSC agents are not yet deployed.
- The published Altana SDK 0.7.0 artifact is GPL-3.0-or-later; distribution obligations and conflicting repository metadata require resolution.
- CI actions are SHA-pinned and source-boundary secret, digest-pinned real-PostgreSQL, and production-license inventory gates exist; a standard SBOM and Altana license resolution remain release gates.
- The npm-account owner must confirm revocation/rotation of the credential exposed in local tool output and review account activity; repository scans cannot close this account-side incident.
