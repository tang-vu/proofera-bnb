# ProofEra evidence

This directory holds reproducible, non-secret evidence for judged claims. It must never contain fabricated transaction hashes, invented benchmark output, private keys, session signers, wallet passwords, API keys, or encrypted keystores.

Each evidence run will use a manifest containing:

- stable run ID and task definition;
- environment and git commit;
- source URLs/contracts, chain ID, wallet/agent public addresses;
- UTC start/end and source-observed timestamps;
- exact inputs, constraints, and software versions;
- raw output file hashes;
- transaction/API receipt identifiers;
- calculation methodology and quality rubric;
- known limitations and whether the result is live, testnet, simulation, or fixture.

Nothing is submission evidence until its manifest validates and its external links have been manually checked.

The `termix/preregistrations/` subdirectory contains exactly three local, schema-validated experiment protocols. Every real input, identity, endpoint, release commit and timed runner that does not yet exist is explicitly `UNBOUND`; both agent and manual states are `NOT RUN`, and publication is false. Their SHA-256 definition digests detect local edits but are not public timestamps, results, receipts or proof of agent advantage. Future runs use new run-ID directories and may not overwrite a protocol after either method starts.

The `development/` subdirectory contains clearly labelled toolchain/readiness records, read-only cross-provider observations, and narrowly scoped testnet transaction evidence. Each file's own `recordType`, status, boundaries and limitations control what it proves. Most development records are not transaction, deployment, performance or live-agent evidence. The sole current deployment exception is `bsc-testnet-pta-deployment-2026-08-12.json`, which records the finalized chain-97 PTA receipt/runtime/token state and explicitly does not prove a pool, price, liquidity, oracle, position, activation, performance result or mainnet action.

For example, `pancake-testnet-code-observation-2026-08-11.json` records matching runtime-code observations while explicitly withholding reviewed-manifest status, `pancake-testnet-eip1898-capability-2026-08-11.json` records exact-block-selector provider support without treating it as an uptime or safety claim, and `pancake-testnet-position-authority-observation-2026-08-11.json` records an exact-block ownership/approval read for a third-party testnet NFT while explicitly denying ProofEra ownership or authority. Its SHA-256-bound raw replay record preserves the exact block header and ABI words; replay with the recorded clock establishes reproducibility, not current freshness. `pancake-testnet-static-context-observation-2026-08-11.json` similarly retains manager immutable and token-decimal words while explicitly withholding token trust and code-identity claims. `pancake-liquidity-quote-rounding-regressions-2026-08-11.json` is calculator regression evidence, not a live quote, transaction, or performance claim. `pancake-v3-selector-paths/` contains deterministic local static-analysis artifacts and no qualifying public locator or reviewer approval. `pancake-v3-testnet-pool-candidates-2026-08-11.json` records a bounded 14-pool rejection with an explicit archive-history gap. `pancake-v3-testnet-wbnb-source-verification-2026-08-11.json` records the exact WBNB source/creation/runtime reproduction. The PTA build record and deterministic pool-preparation logic remain inside the isolated contract subtree; their inert golden fixtures are not the live deployment or pool evidence.

`bsc-testnet-pta-wbnb-pool-readiness-2026-08-13.json` is a historical, read-only, two-provider EIP-1898 snapshot at finalized chain-97 block `124767685`. It records matching retained runtime hashes for PTA, WBNB, Pancake V3 factory, pool deployer and Position Manager; zero factory pool results for the pair at fee tiers `100`, `500`, `2500`, and `10000`; mutable factory-owner/LM-deployer observations; and an empty fee-500 conditional CREATE2 candidate. The linked `bsc-testnet-pta-wbnb-pool-readiness-rpc-transcript-2026-08-13.json` retains exact request parameters and both providers' normalized public result payloads; JSON-RPC envelopes, request IDs and headers are intentionally omitted. Collector-source, Keccak-dependency and canonical-body SHA-256 values bind the files. Offline tests validate retained provider equality, runtime/scalar state, cross-file integrity and non-authorization boundaries; they do not authenticate a public RPC. Later offline compiler evidence closes the candidate's exact init-code/artifact derivation for the retained construction path, but it does not retroactively make this historical candidate a pool or current observation. Neither readiness record contains a transaction envelope, signer, signature, approval, broadcast or receipt. They prove neither the pair's current/future absence nor a pool, price, liquidity, oracle, position, permission, performance, provider authenticity, execution authority, or mainnet action.

`pancake-v3-pool-init-code-provenance-2026-08-13.json` binds the official PancakeSwap source commit/tree/archive and individual source blobs, complete standard-JSON compiler inputs, a SHA-256-pinned native `solc 0.7.6` binary, exact Hardhat/native artifact equality, the 23,566-byte pool creation code and init-code hash, retained pool-deployer/factory runtime identities, and two CREATE2 cross-checks. It closes the exact compiler/artifact blocker for the retained construction path only. The record used no RPC, wallet, signer or transaction and establishes no current chain state, deployed candidate pool, price, liquidity, oracle, approval or execution authority.

`pancake-v3-initializer-selector-review-2026-08-13.json` locally binds direct selector `0x13ead562` to the exact Position Manager source/artifact/runtime path and its factory, deployer and pool call graph. It records the no-deadline and initializer-race boundaries and denies all Multicall outer selectors and nested initializer calldata. It is manual/static analysis, not formal verification. That immutable source artifact still carries its historical pre-publication null fields. The later `pancake-v3-initializer-selector-public-retrieval-2026-08-13.json` records a separate unauthenticated HTTPS GET of the revision-pinned, digest-named public Gist with HTTP `200`, no redirect, `33,327` complete bytes and the exact whole-file SHA-256 `2f78e23ba4892194f2e55c99de479c5a5421329cc4cf992ed2253dd5c0512f02`; the linked publication manifest binds the two records without rewriting reviewed bytes. This establishes the observed publication and byte re-fetch only. Reviewer identity remains null and unauthenticated, so activation, wallet use, signing and execution remain false.

`pancake-v3-pta-wbnb-external-review-request-2026-08-13.json` is a deterministic request package, not a review result. It pins the exact direct-only subject to source commit `00f21c405881a5dc320bddf3c757ba13599b1e71`, exactly the eight implementation files enumerated in that request, the revision-pinned Gist, retained source/evidence hashes, fixed calldata and the conditional pool construction. Delivery remains `not_sent_by_this_bundle`; recipient, reviewer, exact accepted reviewer identity, and Sigstore authentication evidence are all null. The request does not include the later post-claim recheck or submission/reconciliation scaffold. It remains retained old-scope evidence and is not used to claim an external, Sigstore-authenticated or third-party review. Neither it nor any review decision can supply owner authorization, wallet access, signing, broadcast, or execution readiness, and its canonical SHA-256 values are unkeyed integrity checks only.

`pancake-v3-pta-wbnb-owner-designated-internal-review-2026-08-13.json` records the repository owner's designation of two distinct read-only subagent tasks for the exact 21-file pool-init subject at commit `bc7000eee4d9698e272cc9deb7dda5748b34318b` and tree `c63821a1c7b035b0d40221ed8cd6066c69d33041`. The deterministic decision closes only the owner-designated internal technical-review gate for that nonexecuting subject. It explicitly claims no organizational independence, external review, Sigstore evidence, authenticated third-party identity, owner transaction approval, production readiness, custody, signing, broadcast, write, receipt, pool or liquidity. Reviewer task labels and summaries come from the collaboration session and are not cryptographic identity evidence. Any subject, release, chain, transaction or production-composition change requires a new decision.

The root `pnpm test:evidence` gate now includes both review-lane suites and has 68 offline tests across the retained evidence set. They do not refresh the public URL or chain, send the old request, authenticate a reviewer, or authorize the server-only pool coordinator. The published Gist is byte-publication evidence only, not authenticated reviewer evidence or exact owner authorization. Full repository verification is a local consistency gate only; even a fresh pass neither extends a pinned review decision to changed code nor authorizes execution.

A locally reviewed implementation covers the exact pool-init transaction protocol, authorization-receipt validation, signer core, Windows worker, and an append-only signing journal rooted at `%LOCALAPPDATA%\ProofEra\operations\bsc-testnet-pta-wbnb-pool-v1`. Its protected current-user ACL and receipt self-hash checks protect local integrity only; neither creates reviewer identity, owner approval, signing authority, or transaction evidence. The production root now performs read-only recovery first and then requires a policy bound to the exact clean published commit/tree/full runtime manifest, a private runtime-envelope instantiation, and a same-process native-TTY owner confirmation with a fresh 32-byte OS-CSPRNG nonce. A closure-private worker realm consumes its capability only after durable worker start. Reproducing JSON, digests, test capabilities, or journal bytes cannot enter that native realm. The public worker factory and generic raw sender remain fail-closed; only the policy-, owner-, journal- and attestation-bound private bridge can reach the one-send port.

The later submission/reconciliation hardening content-validates the one exact signed transaction and fresh dual-RPC pre-submit snapshot; persists the exact owner-v2 single-broadcast policy in an append-only v2 submission journal; starts from durable signing/submission recovery state after restart; and permits only the winner of durable `submission_started` to approach one private send. After that durable acknowledgement it repeats the fixed dual-RPC terminal state check immediately before send. Ambiguous and restarted outcomes are reconciliation-only, with no resend or replacement. Terminal success requires exact dual-provider transaction, receipt and logs; EIP-1898 post-state at the receipt block; and continuous identical ancestry to the fixed finalized checkpoint 128 blocks later. These controls are executable only when the exact current release has a valid owner-designated review policy and the owner enters the exact bounded TTY confirmation. The retained `bc7000e` decision and old external-review request do not cover this changed release. No exact owner transaction approval, signature, send, receipt, pool, or LP evidence was created by this update.

`bsc-testnet-deployer-wallet-2026-08-12.json` records only the public address and local custody/verification properties of a dedicated chain-97 deployment key. Its encrypted keystore and DPAPI-wrapped random password remain outside the repository. The server-only readiness probe's focused suite and one opt-in local Windows unlock/address check pass, but the probe creates no signer, signature, transaction, or RPC result. No balance lookup, funding, signature publication, transaction or deployment occurred, so the record is not onchain evidence and does not replace the separate Altana passkey/session-wallet ceremony.

`bsc-testnet-pta-deployment-observation-2026-08-12.json` is the earlier read-only state record. At its timestamp, two official BNB testnet endpoints agreed on chain `97`, the finalized block, zero balance and nonce, empty signer/target code, unused predicted CREATE address, gas price/estimate, and the exact 1,826-byte PTA runtime returned by `eth_call`. The record remains `blocked` with `INSUFFICIENT_BALANCE`, contains no secret or raw signed transaction, and is not authenticated provider provenance, signing approval, a broadcast, deployment, or receipt. Its envelope hash is null because no funded envelope was validated. The later deployment does not retroactively turn this historical record into an envelope or transaction.

`bsc-testnet-pta-funding-2026-08-12.json` records the later finalized `0.1 tBNB` transfer, corroborated transaction/receipt/block data, and the then-unused deployer nonce and CREATE target from two official BNB testnet RPCs. The BscScan URL is retained only as a public reference because its Cloudflare challenge prevented automated page parsing. This historical record proves funding at that time, not the subsequent deployment, pool, activation or any mainnet action.

`bsc-testnet-pta-deployment-2026-08-12.json` records the subsequent exact nonce-`0` contract creation. Two official BNB testnet RPCs agree on the finalized receipt and canonical block; code, balance and token state are independently bound to an exact common finalized block with EIP-1898 `blockHash + requireCanonical` because the public endpoints had pruned historical state at the deployment block. The record includes no private key, password or raw signed transaction. It proves the chain-97 PTA deployment and fixed token state only—not an eligible pool, liquidity, price, oracle, LP position, Altana authority, performance result or mainnet action.
