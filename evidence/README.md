# ProofEra evidence

This directory holds reproducible, non-secret evidence for judged claims. It must never contain fabricated transaction hashes, invented benchmark output, private keys, session signers, wallet passwords, API keys, or encrypted keystores.

`altana/preparations/125493138-bsc-testnet-readiness.json` is a read-only, two-provider checkpoint of the exact Altana SDK 0.7.0 BSC-testnet network surface. It retains relay capabilities, runtime hashes, registration fee, SDK file hashes and raw public transcripts. Its own classification denies wallet/signer creation, grants, execution, revocation and receipts.

Each evidence run will use a manifest containing:

- stable run ID and task definition;
- environment and git commit;
- source URLs/contracts, chain ID, wallet/agent public addresses;
- UTC start/end and source-observed timestamps;
- exact inputs, constraints, and software versions;
- raw output file hashes;
- transaction/API receipt identifiers;
- calculation methodology and quality rubric;
- known limitations and whether the result is live, testnet, simulation, or fixture.

Nothing is submission evidence until its manifest validates and its external links have been manually checked.

The `termix/preregistrations/` subdirectory contains exactly three local, schema-validated experiment protocols. Every real input, identity, endpoint, release commit and timed runner that does not yet exist is explicitly `UNBOUND`; both agent and manual states are `NOT RUN`, and publication is false. Their SHA-256 definition digests detect local edits but are not public timestamps, results, receipts or proof of agent advantage. Future runs use new run-ID directories and may not overwrite a protocol after either method starts.

The `development/` subdirectory contains clearly labelled toolchain/readiness records, read-only cross-provider observations, and narrowly scoped testnet transaction evidence. Each file's own `recordType`, status, boundaries and limitations control what it proves. Most development records are not transaction, deployment, performance or live-agent evidence. The sole current deployment exception is `bsc-testnet-pta-deployment-2026-08-12.json`, which records the finalized chain-97 PTA receipt/runtime/token state and explicitly does not prove a pool, price, liquidity, oracle, position, activation, performance result or mainnet action.

For example, `pancake-testnet-code-observation-2026-08-11.json` records matching runtime-code observations while explicitly withholding reviewed-manifest status, `pancake-testnet-eip1898-capability-2026-08-11.json` records exact-block-selector provider support without treating it as an uptime or safety claim, and `pancake-testnet-position-authority-observation-2026-08-11.json` records an exact-block ownership/approval read for a third-party testnet NFT while explicitly denying ProofEra ownership or authority. Its SHA-256-bound raw replay record preserves the exact block header and ABI words; replay with the recorded clock establishes reproducibility, not current freshness. `pancake-testnet-static-context-observation-2026-08-11.json` similarly retains manager immutable and token-decimal words while explicitly withholding token trust and code-identity claims. `pancake-liquidity-quote-rounding-regressions-2026-08-11.json` is calculator regression evidence, not a live quote, transaction, or performance claim. `pancake-v3-selector-paths/` contains deterministic local static-analysis artifacts and no qualifying public locator or reviewer approval. `pancake-v3-testnet-pool-candidates-2026-08-11.json` records a bounded 14-pool rejection with an explicit archive-history gap. `pancake-v3-testnet-wbnb-source-verification-2026-08-11.json` records the exact WBNB source/creation/runtime reproduction. The PTA build record and deterministic pool-preparation logic remain inside the isolated contract subtree; their inert golden fixtures are not the live deployment or pool evidence.

`bsc-testnet-pta-wbnb-pool-readiness-2026-08-13.json` is a historical, read-only, two-provider EIP-1898 snapshot at finalized chain-97 block `124767685`. It records matching retained runtime hashes for PTA, WBNB, Pancake V3 factory, pool deployer and Position Manager; zero factory pool results for the pair at fee tiers `100`, `500`, `2500`, and `10000`; mutable factory-owner/LM-deployer observations; and an empty fee-500 conditional CREATE2 candidate. The linked `bsc-testnet-pta-wbnb-pool-readiness-rpc-transcript-2026-08-13.json` retains exact request parameters and both providers' normalized public result payloads; JSON-RPC envelopes, request IDs and headers are intentionally omitted. Collector-source, Keccak-dependency and canonical-body SHA-256 values bind the files. Offline tests validate retained provider equality, runtime/scalar state, cross-file integrity and non-authorization boundaries; they do not authenticate a public RPC. Later offline compiler evidence closes the candidate's exact init-code/artifact derivation for the retained construction path, but it does not retroactively make this historical candidate a pool or current observation. Neither readiness record contains a transaction envelope, signer, signature, approval, broadcast or receipt. They prove neither the pair's current/future absence nor a pool, price, liquidity, oracle, position, permission, performance, provider authenticity, execution authority, or mainnet action.

`pancake/runs/public-position/116342186-7152618.json` is a create-only BSC-mainnet benefit capture for a public third-party USDT/WBNB fee-500 position. It preserves 12 exact-hash JSON-RPC exchanges and the raw public LP-agent A2A request/response with SHA-256 bindings. The agent identified that the in-range position was one tick above its lower boundary and returned `insufficient_evidence` because rebalance economics were absent. The record explicitly denies ProofEra ownership, authority, performance, registration, hiring, execution and transaction-receipt claims.

`pancake-v3-pool-init-code-provenance-2026-08-13.json` binds the official PancakeSwap source commit/tree/archive and individual source blobs, complete standard-JSON compiler inputs, a SHA-256-pinned native `solc 0.7.6` binary, exact Hardhat/native artifact equality, the 23,566-byte pool creation code and init-code hash, retained pool-deployer/factory runtime identities, and two CREATE2 cross-checks. It closes the exact compiler/artifact blocker for the retained construction path only. The record used no RPC, wallet, signer or transaction and establishes no current chain state, deployed candidate pool, price, liquidity, oracle, approval or execution authority.

`pancake-v3-initializer-selector-review-2026-08-13.json` locally binds direct selector `0x13ead562` to the exact Position Manager source/artifact/runtime path and its factory, deployer and pool call graph. It records the no-deadline and initializer-race boundaries and denies all Multicall outer selectors and nested initializer calldata. It is manual/static analysis, not formal verification. That immutable source artifact still carries its historical pre-publication null fields. The later `pancake-v3-initializer-selector-public-retrieval-2026-08-13.json` records a separate unauthenticated HTTPS GET of the revision-pinned, digest-named public Gist with HTTP `200`, no redirect, `33,327` complete bytes and the exact whole-file SHA-256 `2f78e23ba4892194f2e55c99de479c5a5421329cc4cf992ed2253dd5c0512f02`; the linked publication manifest binds the two records without rewriting reviewed bytes. This establishes the observed publication and byte re-fetch only. Reviewer identity remains null and unauthenticated, so activation, wallet use, signing and execution remain false.

`pancake-v3-pta-wbnb-external-review-request-2026-08-13.json` is a deterministic request package, not a review result. It pins the exact direct-only subject to source commit `00f21c405881a5dc320bddf3c757ba13599b1e71`, exactly the eight implementation files enumerated in that request, the revision-pinned Gist, retained source/evidence hashes, fixed calldata and the conditional pool construction. Delivery remains `not_sent_by_this_bundle`; recipient, reviewer, exact accepted reviewer identity, and Sigstore authentication evidence are all null. The request does not include the later post-claim recheck or submission/reconciliation scaffold. Its generator, test, and retained artifact remain pinned to the historical 45-second envelope cap and are not a producer or evidence source for the revised timing contract. It remains retained old-scope evidence and is not used to claim an external, Sigstore-authenticated or third-party review. Neither it nor any review decision can supply owner authorization, wallet access, signing, broadcast, or execution readiness, and its canonical SHA-256 values are unkeyed integrity checks only.

`pancake-v3-pta-wbnb-owner-designated-internal-review-2026-08-13.json` records the repository owner's designation of two distinct read-only subagent tasks for the exact 21-file pool-init subject at commit `bc7000eee4d9698e272cc9deb7dda5748b34318b` and tree `c63821a1c7b035b0d40221ed8cd6066c69d33041`. The deterministic decision closes only the owner-designated internal technical-review gate for that nonexecuting subject. It explicitly claims no organizational independence, external review, Sigstore evidence, authenticated third-party identity, owner transaction approval, production readiness, custody, signing, broadcast, write, receipt, pool or liquidity. Reviewer task labels and summaries come from the collaboration session and are not cryptographic identity evidence. Any subject, release, chain, transaction or production-composition change requires a new decision.

The root `pnpm test:evidence` gate now includes both review-lane suites and has 68 offline tests across the retained evidence set. They do not refresh the public URL or chain, send the old request, authenticate a reviewer, or authorize the server-only pool coordinator. The published Gist is byte-publication evidence only, not authenticated reviewer evidence or exact owner authorization. Full repository verification is a local consistency gate only; even a fresh pass neither extends a pinned review decision to changed code nor authorizes execution.

A locally reviewed implementation covers the exact pool-init protocol, Windows worker, and append-only journals. Generation-1 and generation-2 namespaces are immutable predecessors; active recovery uses `bsc-testnet-pta-wbnb-pool-v3`. Protected ACL/self-hash checks are local integrity evidence only, not reviewer identity, owner approval or authority. The only supported entry remains the exact absolute phase-minus-one command and audited triplet; direct Node is unsupported and the pnpm wrapper is fail-only.

Phase minus one hardcodes and requires the exact repository root, its own exact absolute path, and the absolute phase-zero path. It first clears its Process environment and installs exactly 13 pinned non-secret values—11 real-host values plus two WebSocket native-addon disable guards. It then clears only the console input mode's `ENABLE_QUICK_EDIT_MODE`, sets `ENABLE_EXTENDED_FLAGS`, and verifies every other bit through the pinned .NET Framework console methods before starting the pinned Node phase zero. It does not read, flush, synthesize, or write input bytes, and fails closed without an exact console handle/mode result. Phase zero accepts only those exact names/values, validates the audit triplet, pinned Node/direct-Git executable files, clean published commit/tree, release-source blobs including the phase-minus-one script, pinned dependency trees and ignored runtime topology, and working directory, then spawns phase one under the same minimal environment. Apart from the two console-mode bits and its own process environment, phase minus one is operationally read-only, and phase zero is read-only; journal recovery is the child's first application-stateful or external operational action. The trust boundary still includes both the phase-minus-one bytes before their later manifest check and phase-zero bytes before/during their self-check, all PowerShell 5.1/.NET startup and ambient environment until scrub, Windows/filesystem/process semantics, the pinned executable files, and their loaded OS/runtime/DLL installation. The later self-check and Git-blob checks are evidence rather than a root of trust. Complete installations are not hashed, and there is no active same-user, preload/parent-provenance, or comprehensive concurrent-tamper defense.

The fresh path then requires a policy bound to that exact release identity, private runtime-envelope instantiation, and a same-process native-TTY owner confirmation with a fresh 32-byte OS-CSPRNG nonce. Before durable `worker_started`, custody preactivation checks only fixed paths, file kind, `lstat`/`realpath`, and current-user ACL; it does not open the custody artifacts, invoke DPAPI, unlock the keystore, or reconstruct a key. The authority record identifies the ACL holder only, not the key owner; the expected signer is established by the post-claim signed-transaction attestation. A closure-private worker realm consumes its capability only after durable worker start. Reproducing JSON, digests, test capabilities, or journal bytes cannot enter that native realm. The public worker factory and generic raw sender remain fail-closed; only the policy-, owner-, journal- and attestation-bound private bridge can reach the one-send port.

One historical production-path exercise used exact audited release commit `1a89ddae5e1f575b39dda6134d3ccedaddda0adf`, tree `b28a12f78861183ece5ff1c3fce36c0abf094239`, runtime-manifest SHA-256 `0xd5212c7563d5d3e3b10d461b5dedcc1552766262c1c87a7057b4dc2876ed0ce0`, and a canonical policy whose digest was `0x9357b560a91b1c41ef8ef84ea39001c206f50fc9839a55102f7916152160a189`; both exact-release audits returned `GO`. The policy occupied `8,670` bytes and `11,560` unpadded-base64url characters, while its complete v1 single-line frame occupied `11,713` characters. Windows Console truncated the frame, so policy admission failed closed and the owner-confirmation stage was not reached. Only read-only recovery/journal inspection may have occurred before that rejection. The exercise made no fresh RPC call and produced no custody-artifact access, DPAPI operation, custody unlock, signature, new transaction hash, send, receipt, pool, or liquidity evidence. Because the source subsequently changed to transport v2, this release triplet and policy are immutable historical inputs, not authority for the changed source.

The current v6 challenge/frame domains retain the bounded transport state machine introduced in v2: a single nonce-bound ASCII `BEGIN` line, exact ordered nonce-bound `CHUNK` lines, and a nonce-bound `END` line under one absolute five-minute deadline. Every line binds its line index, chunk count, policy byte length, and lowercase raw policy-byte SHA-256; each chunk also binds its chunk index. The reader hard-rejects content above `4,096` bytes, while maximum valid worst-case construction is exactly `2,618` content bytes and is tested conservatively at no more than `2,700`. The complete transport is limited to `102,400` bytes, policy bytes to `1..65,536`, and each non-final payload to exactly `2,304` unpadded-base64url characters; the derived count is at most `38`. The historical `11,560`-character policy maps to six chunks and eight total bounded lines but remains non-authorizing. A one-off local Windows Console observation motivated these conservative constants, but its harness and result are not retained and it is not reproducible repository evidence. Repository unit tests establish only the transport arithmetic and parser behavior. The decoder accepts only exact LF or CRLF framing, exact order/count/length/hash and canonical base64url, and rejects retired v1/v2/v3/v4/v5 domains, truncation, duplicate/missing/reordered lines, control bytes, and trailing or buffered input before policy admission, fresh RPC, custody access, or owner authority. This is a parser/admission invariant, not proof that no input was preloaded into the OS queue and not protection from malicious code running as the same user.

A later exact v2 exercise used release commit `36f6e5e7fa8b4b5ccf255a6210afa2d25c25afa5`, tree `54ed5c2a8f754e79080528a2ce25669a6532a66b`, runtime-manifest SHA-256 `0x25e5aedb6d73e6fff416803ce9d42737d9124e525e0b81bf906321f4d06258d4`, and canonical policy digest `0xd1a33479a607d744a51ff8d6d3df8772f41ec2f1363911d2655f926c754c3b38`. The strict TTY-v2 reader admitted that policy. Recovery probes found both signing and submission journals absent; a fresh read-only preflight against the two fixed official RPCs succeeded and the exact owner challenge was displayed. No exact owner confirmation was accepted. Subsequent attempts stopped fail-closed with `POLICY_FRAME_INVALID`, `CEREMONY_IO_FAILED`, and `OWNER_CONFIRMATION_INVALID`. Across those exercises there was no custody-artifact access, DPAPI operation, custody unlock, signature, new transaction hash, send or broadcast, receipt, pool, or liquidity. This is a one-off local terminal observation whose raw output and harness were not retained; it is not reproducible repository evidence or a gate. The negative custody/sign/send claims combine the observed fail-closed result with code ordering and are not a retained artifact. The timing source then changed, so this exact release triplet and policy are historical and non-authorizing.

The current runtime timing record separates an exact `300`-second coordinator envelope, at most `240` seconds for owner entry, an exact `120`-second execution-authority lifetime, and a `30`-second post-claim freshness bound. The owner-entry cap is further bounded by preserving the full 120-second authority reserve. Generation 5 additionally requires at least `60` seconds remaining and at most `60` seconds elapsed before claim, plus a `20`-second reserve after recheck. It binds the exact generation-4 terminal under owner-v8 domains and uses TTY challenge/frame v6. Only an exact owner-byte match permits the internal clock to capture `confirmedAt`; retained journal/policy bytes never recreate authority.

The next production-path exercise is not a file under `evidence/`; it was a one-off local operational observation. It used audited release commit `336af2967286795dc7703fff85034c71b8e84b5c`, tree `86cc383388982dac1a2bea430f54d54e56bb6cf9`, runtime-manifest SHA-256 `0xa1cda6fcf00f8a7d2b9a679cfb9b3fc28aa60674dae89c7dbfc032bdbcff5bdd`, and canonical policy digest `0x8ddae3b13ee64ff5f983ce30d06c84d671e0a0ca029f75b5482de6b34b18ba54`. The admitted `8,826` policy bytes had raw SHA-256 `0x87de481e35a0d8fe6c503f4a7c832d665699ac9f20aa79eb5c40471d79e71a45` and `11,768` unpadded-base64url characters across six chunks. The owner supplied the exact v4 confirmation and the local legacy journal durably retained only its `1,123`-byte slot-1 claim, whose raw SHA-256 is `0xf10e90eb836a94446ace100bbc9a6fc5de6cc35b1d82e4d10fb4736ef8559e32`. QuickEdit/selection then froze the console during the post-claim RPC recheck; after the selection was cancelled after authorization expiry, the observed terminal code was `POST_CLAIM_RECHECK_OUTCOME_UNKNOWN` and no transaction hash existed. Exact journal inspection and code ordering show no worker authorization/start, custody-artifact access, DPAPI operation, unlock, signature, send, receipt, pool, or liquidity, and the submission namespace was exactly empty. Raw console output and the operational harness were not retained, so the owner/terminal observation is not reproducible repository evidence or a gate. A later dual-RPC no-effect observation was live but non-retained, so neither it nor this narrative is reproducible chain evidence. The expired v4 confirmation is historical and cannot authorize recovery generation 2.

Generation 2 fenced the exact generation-1 claim and later produced one exact claim-only incident of its own. In a one-off non-retained exercise, release `655187f2b425c40839803950257e1d5a5c4f8d98` accepted owner-v5 confirmation and retained only `01-claim.v2.json`: `1,362` bytes, raw SHA-256 `0x613df995936c3ccfff56e5da5588906f1bd28340ae8297eb08524274b9b8e1c3`. The observed result was `POST_CLAIM_RECHECK_OUTCOME_UNKNOWN`; journal inspection and code ordering show no worker authorization/start, custody/DPAPI/unlock, signature, submission, transaction hash, receipt, pool, or liquidity. Raw output/harness were not retained, so this narrative is not reproducible evidence or authority.

Generation 3 subsequently became a third claim-only incident. A one-off local run accepted exact owner-v6 bytes and retained only `01-claim.v3.json`: exactly `1,362` bytes with raw SHA-256 `0x7ff780a8f0ac1a1f8ff7bced5d858259f918cdb1891c684aa208b6bca31c9585`. Journal inspection and code ordering show no slot 2, worker authorization/start, custody access, signature, submission, transaction hash, receipt, pool, or liquidity. The exact failure code/output was not retained, so expiry is only a plausible timing diagnosis and is not asserted as fact or evidence.

Generation 4 is historical. A one-off local run accepted owner-v7 confirmation and durably retained an exact `failed_before_worker` claim with raw SHA-256 `0xd5fc6da9f853c621f4f407c9d8a729f898c0297720bc50817e633fa538967f36` and terminal transition with raw SHA-256 `0x2e0570423b1217f1dab6fa8cdb91a0a75b2d78023bacc611a6c81017d0033bab`. The terminal code was `POST_CLAIM_RECHECK_OUTCOME_UNKNOWN`. Exact two-slot local-journal ordering proves there was no worker authorization/start or signature. Separate exact-empty submission-v3 inspection plus code ordering proves there was no transaction submission/send; the same ordering supports no custody-secret access or DPAPI/unlock. Raw console output and the operational harness were not retained, so the owner interaction and terminal observation are not reproducible repository evidence or chain evidence.

Generation 5 is the current incident-specific recovery path. It binds the exact generation-4 terminal directly and separately rechecks exact-empty submission-v3; it has no Invocation A, no new no-effect proof, and no fence observation reused as its execution envelope. It requires a fresh distinct envelope after the terminal and rereads eight isolated namespaces: signing generations 1 through 5 and submission v2 through v4. Policy/runtime is v5, TTY is v6, owner command/text/confirmation is v8, signing and broadcast are v5, and current submission uses directory `bsc-testnet-pta-wbnb-pool-submission-v4` with v6 journal schema/files.

The current submission/reconciliation stack content-validates the one exact generation-5 signed transaction and fresh dual-RPC pre-submit snapshot, persists owner-authorization schema v6 and recovery binding in append-only v6 records, and permits only the durable-start winner to approach one private send. Known post-claim failures before worker authorization durably record `failed_before_worker`; ambiguity/restart remains non-retryable and reconciliation-only. These controls require a newly committed/pushed generation-5 release, new exact audits/policy, and separate owner-v8 confirmation. No generation-5 owner confirmation, signature, send, receipt, pool, or LP evidence exists in this directory.

`bsc-testnet-deployer-wallet-2026-08-12.json` records only the public address and local custody/verification properties of a dedicated chain-97 deployment key. Its encrypted keystore and DPAPI-wrapped random password remain outside the repository. The server-only readiness probe's focused suite and one opt-in local Windows unlock/address check pass, but the probe creates no signer, signature, transaction, or RPC result. No balance lookup, funding, signature publication, transaction or deployment occurred, so the record is not onchain evidence and does not replace the separate Altana passkey/session-wallet ceremony.

`bsc-testnet-pta-deployment-observation-2026-08-12.json` is the earlier read-only state record. At its timestamp, two official BNB testnet endpoints agreed on chain `97`, the finalized block, zero balance and nonce, empty signer/target code, unused predicted CREATE address, gas price/estimate, and the exact 1,826-byte PTA runtime returned by `eth_call`. The record remains `blocked` with `INSUFFICIENT_BALANCE`, contains no secret or raw signed transaction, and is not authenticated provider provenance, signing approval, a broadcast, deployment, or receipt. Its envelope hash is null because no funded envelope was validated. The later deployment does not retroactively turn this historical record into an envelope or transaction.

`bsc-testnet-pta-funding-2026-08-12.json` records the later finalized `0.1 tBNB` transfer, corroborated transaction/receipt/block data, and the then-unused deployer nonce and CREATE target from two official BNB testnet RPCs. The BscScan URL is retained only as a public reference because its Cloudflare challenge prevented automated page parsing. This historical record proves funding at that time, not the subsequent deployment, pool, activation or any mainnet action.

`bsc-testnet-pta-deployment-2026-08-12.json` records the subsequent exact nonce-`0` contract creation. Two official BNB testnet RPCs agree on the finalized receipt and canonical block; code, balance and token state are independently bound to an exact common finalized block with EIP-1898 `blockHash + requireCanonical` because the public endpoints had pruned historical state at the deployment block. The record includes no private key, password or raw signed transaction. It proves the chain-97 PTA deployment and fixed token state only—not an eligible pool, liquidity, price, oracle, LP position, Altana authority, performance result or mainnet action.
