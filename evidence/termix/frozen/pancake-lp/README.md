# Frozen Pancake LP inputs

`116342186-7152618.canonical-json` is the compact shared-input candidate for
TermiX task 01 protocol v2. Its logical canonical JSON SHA-256 (excluding the
single repository line terminator) is
`3459eb2566621c4d74acef68c84849e59b74214c7a21d7d20b8bbc6352dda945`.

The bundle binds the retained public-position evidence by repository path and
exact file SHA-256, the BSC-mainnet block/hash, pool, manager, public position,
expected `slot0` tick, fixed RPC origin, and the byte-equivalent request sent
to the LP agent. The source position is owned by an unrelated third party.
This input grants no authority and is not itself a benchmark run, hire,
performance record, transaction, or receipt.

Both future methods must receive the same logical bytes. The agent lane and
manual lane each require an exact-hash `slot0` API exchange during the timed
run. The agent lane additionally requires a registered ERC-8004 identity and
independently verified ProofEra hire receipt before either network call.
