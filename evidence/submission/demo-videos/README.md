# Demo video evidence

`scripts/capture-public-demo-video.mjs` writes create-only exact-release artifacts beneath
`<source-commit>/<mode>/`.

`rehearsal` output is a silent technical recording and never closes the demo gate. `final` output is
allowed only after the five non-demo objective gates are verified and requires committed narration,
one audio stream, full media decoding, and a 240–330 second duration. Neither mode is a hackathon
submission receipt.
