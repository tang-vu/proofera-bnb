# Altana grant intents

This directory accepts reviewed, public-only BSC-testnet grant intents. A retained intent must contain
the wallet address, public session descriptor, exact call/spend permissions, expiry and
`registerInKeystore: true`; it must never contain a private key, KMS credential, passkey material or
signing capability. No intent is currently retained.
